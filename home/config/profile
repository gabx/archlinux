# File ~/.config/profile
# shell customization information
# These variables are only defined for the current instance of the shell
# and are not available to programs started by the shell.
# Last modified 2013-11-09

## Any setting will take precedent on .initzsh ##


# we need a different java bin to build android
# JAVA_HOME=/opt/java7

# use xdg directories if file exists	
if [[ -e "$XDG_CONFIG_HOME/user-dirs.dirs" ]] ; then
. "$XDG_CONFIG_HOME/user-dirs.dirs" 
fi

# loaction of vimrc
export VIMINIT=' let $MYVIMRC=$XDG_CONFIG_HOME/vim/vimrc | source $MYVIMRC'	


											